# Public URL, exactly as opened in the browser (no trailing path).
APP_ORIGIN=http://localhost:3000
HOST=127.0.0.1
PORT=3000
DATA_DIR=./data
# Optional for localhost; REQUIRED (16+ characters) for 0.0.0.0 / internet.
# Browser HTTP Basic login: any username, this password.
APP_PASSWORD=
MAX_ACTIVE_EDITS=4
